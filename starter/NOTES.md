The best configuration maximizes the 2,000,000 parameter limit by employing a 6-layer Llama-style decoder-only transformer with a hidden dimension of 256 and 8 query heads. Parameter efficiency is heavily optimized through Multi-Query Attention, which reduces Key and Value projections to a single shared head to save matrix overhead. Weight tying explicitly binds the input embedding layers and the final output language modeling projection, ensuring the 1024 vocabulary size consumes minimal parameter space. The custom Byte Pair Encoding tokenizer delivers a massive 2.66x compression ratio on the mixed English-Hindi text, effectively multiplying the representational density of the 128-block context window. SwiGLU activations with an inner hidden dimension of 180 provide strong non-linear capacity within the feed-forward blocks. RMSNorm layers accelerate compute speeds over standard LayerNorm while preserving numerical stability across all residual streams. Relative positioning mechanics are handled dynamically via Rotary Positional Embeddings rather than wasteful absolute learned matrices. The optimization sequence leverages an aggressive Cosine Annealing schedule with a 200-step linear warmup peaking at a learning rate of 4e-3. Finally, gradient accumulation simulates a stable batch size of 64 on CPU, avoiding local minima and guaranteeing uniform token coverage.